//wap to calculate area of rectangle 
#include <stdio.h>
int main()
{
	int l,b,area;
	printf("Enter  the value of l and b: ");
	scanf("%d%d",&l, &b);
	area = l*b;
	printf("Area of rectangle : %d",area );
}

